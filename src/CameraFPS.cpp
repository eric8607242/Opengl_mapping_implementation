#include "CameraFPS.h"

void CameraFPS::UpdateView() {
    // initialize roll, pitch and yaw matrix
    glm::mat4 matRoll = glm::mat4(1.0f);
    glm::mat4 matPitch = glm::mat4(1.0f);
    glm::mat4 matYaw = glm::mat4(1.0f);
}